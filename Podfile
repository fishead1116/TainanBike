# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'TainanBike' do
  use_frameworks!
  pod 'Alamofire'
  pod 'SwiftyJSON'
  pod 'GoogleMaps'
  # Pods for TainanBike

  target 'TainanBikeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TainanBikeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
